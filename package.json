{
  "name": "metalist",
  "version": "1.0.0",
  "description": "A minimalist note-taking application with a focus on server-side rendering and efficient data synchronization.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "node --test tests/unit/*.test.mjs",
    "render-diagrams": "mkdir -p docs/diagrams/png && for file in docs/diagrams/*.md; do npx mmdc -i \"$file\" -o \"docs/diagrams/png/$(basename \"$file\" .md).png\"; done",
    "test:browser": "node scripts/browser-smoke.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.9.0",
    "puppeteer": "23.11.1"
  }
}
